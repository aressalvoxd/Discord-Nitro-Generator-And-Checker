# ARES SALVO - Discord Nitro Kod Oluþturucu Ve Kontrol Aracý

## Gerekli Olanlar
Bu yazýlýmý çalýþtýrmak için aþaðýdaki gereksinimleri karþýladýðýnýzdan emin olun:

1. **Python**:
   - Python 3.6 veya daha yeni bir sürüm kurulu olmalýdýr.
   - Python'un resmi web sitesinden (https://www.python.org/downloads/) indirilebilir.

2. **Gerekli Kütüphaneler**:
   - `colorama`: Terminalde renkli çýktýlar almak için gereklidir.
   - `http.client`, `random`, `string`, `ssl`, `urllib.parse`: Bu kütüphaneler Python'un standart kütüphanesinde bulunmaktadýr.

## Kurulum Adýmlarý
1. **Python'un Kurulumu**:
   - Python'u indirip kurun. Kurulumdan sonra terminal veya komut istemcisinde `python --version` veya `python3 --version` komutunu çalýþtýrarak kurulumun baþarýlý olup olmadýðýný kontrol edin.

2. **Gerekli Kütüphanelerin Yüklenmesi**:
   - Terminal veya komut istemcisini açýn.
   - Aþaðýdaki komutu çalýþtýrarak `colorama` kütüphanesini yükleyin:
     ```
     pip install colorama
     ```

3. **Yazýlýmýn Ýndirilmesi**:
   - Kodlarý bir `.py` dosyasýna yapýþtýrýn. Örneðin, `salvo.py` olarak adlandýrabilirsiniz.

## Kullaným
1. **Programý Çalýþtýrma**:
   - Terminal veya komut istemcisini açýn.
   - Programýn bulunduðu dizine gidin. Örneðin:
     ```
     cd /path/to/your/program
     ```
   - Aþaðýdaki komut ile programý çalýþtýrýn:
     ```
     python salvo.py
     ```
   - Alternatif olarak, bazý sistemlerde `python3` komutu kullanýlabilir:
     ```
     python3 salvo.py
     ```

2. **Seçeneklerin Kullanýlmasý**:
   - Program çalýþtýðýnda, iki seçenek sunulacaktýr:
     1. Rastgele Nitro promo kodu oluþtur ve kontrol et.
     2. Bir `.txt` dosyasý ile verilen promo kodlarýný kontrol et.
   - Seçiminizi yapýn ve talimatlarý izleyin.

3. **.txt Dosyasý Kullanýmý**:
   - Kodlarý içeren bir `.txt` dosyasý oluþturun. Her kodu yeni bir satýra yazýn.
   - Programdan bu dosyanýn adýný girerek kodlarý kontrol edebilirsiniz.

## Ýletiþim
discord : aresslv
instagram : aressalvo
Bu yazýlým ile ilgili herhangi bir sorun veya öneri için iletiþime geçebilirsiniz.
